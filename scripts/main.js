UI.init({
  jslider: new JSlider(),
  inputs: {
    $id:       $('#id'),
    $count:    $('#count'),
    $widths:   $('#widths'),
    $width:    $('#width'),
    $height:   $('#height'),
    $duration: $('#duration'),
  },
}); 
